services:
  - type: web
    name: lord-backend
    env: python
    buildCommand: ""
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false